// Name: Corey Beaumont
// Course: CIS 544-M401 Object Oriented Programming in C++
// Org: Syracuse University
// Task: Final Project
// Date: 3/01/21
// File: main.cpp

#include <iostream>
#include <string>
#include <ctime>

#include <random>

int main() {
std::random_device rd;
std::mt19937 rng(rd());
std::uniform_int_distribution<int> uni(1, 100);

  while (true) {
    std::time_t result = std::time(nullptr);
    std::string time  =  std::asctime(std::localtime(&result));

    // Test 1
    std::cout << time << "\r" << std::flush; // Does not overwrite, but should/I want it to

    // Test 2
    // std::cout << uni(rng) << "\r" << std::flush; // Does overwrite as expected

    // sleep
    std::chrono::milliseconds timespan(1000);
    std::this_thread::sleep_for(timespan);
  }
}
